# 完整开发执行清单

用户于 2026-09-20 授权持续开发直至全部功能完成。以 01、05、07、09、11、12 号规格为验收依据；每项分为实现、自动验证、真实环境验证，不以占位页面计为完成。

## 功能代码清单

下列勾选表示功能实现及对应本地合同测试已经具备；不代表真实平台准入或正式发布验收。最终构建与跨系统结果汇总在 [首版功能与集成验证](23-首版功能与集成验证.md)。

- [x] D2 实现及夹具：真实内容模型、来源接口、字符串作品/章节/页面标识。
- [x] D2 实现及夹具：哔咔搜索/探索/分类/详情、完整章节与页面分页，阅读与历史闭环。有效账号真实内容验收待进行。
- [x] D4 基础：持久收藏、真实历史、逐作品阅读偏好与数据迁移。
- [x] D3：JM 协议、登录/匿名内容、图片重排及线路配置。
- [x] D3：EH/EX 内容、网页会话、权限/额度与原图。
- [x] D3：Hitomi 索引搜索、动态图片规则与格式。
- [x] D3：绅士漫画页面解析、内容、账号与可信域名。
- [x] D3：nhentai v2 认证、搜索、详情、阅读及权限。
- [x] D4：持久下载队列、暂停/恢复/重启、离线、目录授权、缓存边界。
- [x] D4：数据导出/校验/预览/合并、删除标记与 WebDAV 条件写入/冲突处理。
- [x] 设置：来源排序/签到去重、内容筛选、亮度/背景/方向、刷新率、日志与缓存。
- [x] D5：GitHub 更新检查、可信清单、APK 下载校验与系统安装入口。
- [x] D5 本地验证：Release 优化、API 26/36、宽度/方向/字体、阅读后台释放与持久数据恢复回归。实体性能/系统下载配额仍见外部缺口。
- [ ] D5 外部验收：有效平台账号、实体手机、正式签名和发布端配置。

## 外部条件

目前没有提供漫画平台真实账号、WebDAV 测试服务或正式发布签名。开发和夹具验证继续进行；需要时通过 App 的受限输入界面联调，不在聊天、代码或文档中保存用户口令。当前 GitHub 源码仓库是 private，免登录自动更新的分发方案需要实际可公开访问的发布端，不能在 APK 内置 PAT。

## 执行记录

- 2026-09-20：已有 0.2.3-alpha 登录基础，30 项 JVM、33 项模拟器回归通过。开始 D2 内容模型和哔咔内容链路。
- D2 定向验证 14 / 14 通过，包括协议分页、数据库重开、真实字符串标识的 UI 闭环和既有阅读控制。已修复初始微型缩略图比例污染续读偏移的问题。随后 D2 全量 JVM 36 / 36、Android 41 / 41 常规测试通过，显式联网测试未启用。

- JM 协议、内容、会话验证和图片重排已实现；JVM 总计 41 项、定向模拟器 11 / 11 通过。当前补齐逐作品阅读偏好、显示控制与数据库升级，回归执行中。详情见 [内容接入与 JM 验证](19-内容接入与JM验证.md)。

- 阅读显示偏好定向 12 / 12、JVM 42 / 42 通过。新增逐作品模式/亮度/背景/方向、系统亮度和刷新率生命周期、左右边缘翻页；Room 1→2 迁移保留旧收藏。修复点击回调捕获旧视口宽度的问题。nhentai v2 匿名接口结构实测已通过，继续接入。

- nhentai v2：匿名/Key/User 分开接入，网页检测、手动凭据验证、分页与实际页码、可信 CDN；与 JM 和哔咔账号页合计 15 项定向模拟器回归通过。真实用户权限仍待验收。
- EH/EX、绅士漫画：新增 HTML 结构解析、EH/EX 原站标识和游标分页、惰性图片解析、原图回退与额度停止；EH 网页/手动 Cookie 验证和独立 EX 权限验证。HT 发布页候选发现、原点隔离登录、完整阅读数据解析。与阅读控制合计 20 项模拟器回归通过。
- Hitomi：范围索引、64 位节点位置、关键词/标签交集、图库数据、严格解析动态 gg 规则和 WebP 图片链路。JVM 总计 53 项通过；EH/HT/Hitomi 合计 20 项定向模拟器测试通过，其中 3 项为显式开启的匿名结构联调，未下载漫画图片。下一步进入持久下载、离线及数据同步。
- 实测修复：HT 阅读数组尾逗号按字符串外的 token 处理；EH 不再通过搜索参数修改站点显示模式；Hitomi 正则兼容 Android ICU 与 JVM。详细结果见 [六来源接入验证](20-六来源接入验证.md)。
- D4：持久下载、断点、前台服务、离线入口、SAF、真实缓存额度已实现；Android 16 专项 11 / 11，加阅读/格式 4 / 4；Android 8.0 核心专项 16 / 16。备份事务与 WebDAV 继续实施，详见 [下载离线与数据能力验证](21-下载离线与数据能力验证.md)。
- 最终检查点：首版功能代码实现完成，JVM 56 / 56；API 26、36 各 114 项（109 通过、5 显式外部测试默认跳过、0 失败），SAF 另 1 / 1。Debug、R8 Release 和 Lint 通过；本地开发签名包覆盖后数据保留及重启检查通过。具体日志、安装包与外部验收边界见 [23 号文档](23-首版功能与集成验证.md)。
