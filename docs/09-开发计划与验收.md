# 09 · 开发计划与验收

## 1. 阶段与完成条件

| 阶段 | 内容 | 完成条件 |
|---|---|---|
| D0 文档/交互 | 本套文档、HTML、竞品证据 | 关键页面可点击、约束一致、用户评审 |
| D1 技术验证 | Kotlin 工程；阅读组件；代理/WebView；六来源小样联调 | 普通/长图可读，网络路径证明，来源认证合同有记录 |
| D2 首个闭环 | 优先 picacg 或 JM 中实测可用者：搜索→详情→阅读→历史 | 不依赖演示数据；会话过期可恢复 |
| D3 来源完善 | 逐个补齐剩余五组；EH/EX 权限、Hitomi、NH v2 | 六组全部通过来源准入矩阵 |
| D4 本地能力 | 收藏、下载、离线、备份、缓存管理、WebDAV 和设置 | 飞行模式可读、进程重启可恢复、不丢书架，导入/同步冲突不静默覆盖 |
| D5 稳定交付 | 真机性能、兼容、安全存储、GitHub Releases 更新 | 所有 P0 验收通过，发布资产完整，旧版检测并覆盖升级通过 |

阶段是顺序建议，不是工期承诺。六个平台的账号和网络可用性会影响联调周期。D2 先接一站不改变六来源 v1.0 范围。

### 当前执行状态（2026-09-20）

- D0 和原生界面基线已完成评审及实现，版本 `0.1.2-ui`。
- D1 阅读器子阶段进入 `0.2.0-alpha`：图片组件、缩放、音量键、自动翻页、常亮、预加载、Room 阅读进度，详见 [D1 阅读器功能与验证](14-D1阅读器功能与验证.md)。
- D1 网络与认证基础已完成子阶段：代理设置与连接测试、统一网络 generation、加密存储、候选会话验证合同和受控 WebView，详见 [D1 网络与认证基础](16-D1网络与认证基础.md)。
- `0.2.3-alpha` 开放哔咔原生账号登录联调：匿名地址/签名探测、登录后用户资料验证、恢复/失效/清除会话，详见 [哔咔账号登录联调](17-哔咔账号登录联调.md)。Windows 与 Android 模拟器匿名探测已通过；有效真实账号尚待用户在 App 内联调。
- `0.3.0-alpha`：D1–D5 首版功能代码已覆盖六来源核心链路、账号与网络、完整阅读器、书架、持久下载/离线、备份/WebDAV、设置/日志和更新模块。详见 [完整执行清单](18-完整开发执行清单.md)及 [集成验证](23-首版功能与集成验证.md)。
- 来源合同、合成图片、事务/网络和模拟器测试已经建立；真实账号完整准入、真实图片、实体机性能及正式发布仍未验收，不能将实现完成等同于所有 P0 发布门槛已通过。
- 源码已托管至 `ddmoyu/PiComic` 私有仓库；正式签名与 Releases 发布仍未完成。
- 历史 `0.2.3-alpha` 验证保留于 17 号文档；当前版本的最终数字统一见 23 号文档，避免把不同检查点累加为一次通过结果。

## 2. 需求到验收的映射

| 需求 | 必测行为 |
|---|---|
| SRC-01 / EXP-01 / CAT-01 | 探索/分类平台 Tab、固定顶栏、纯图标底栏；六来源列表/详情/独立分类，切平台保留页面与位置、无串数据 |
| SEA-01 | 中文/标签/空关键词/无结果/分页/取消/乱序返回 |
| DET-01 | 多章节、全册、已删除作品、历史恢复 |
| RDR-01/02 | LTR/RTL/连续、长图缩放、跳页、旋转、退后台/进程终止 |
| LIB-01 | 收藏增删/撤销、历史恢复、数据迁移、不要求平台登录 |
| DL-01 | 暂停/继续/重启、部分失败、磁盘不足、认证过期、飞行模式 |
| AUTH-01 | 账号管理分平台资料/重新登录/退出；WebView 自动获取/验证/返回，缺失/失败/取消与迟到回调；有效/错误凭据、过期、挑战、撤销 Key、并发恢复、退出竞态 |
| NET-01 | FollowSystem、系统 VPN/代理、自定义 HTTP、WebView 同路由、代理断连无直连回退 |
| SET-01 | 明暗模式、字体放大、备份合并、缓存/下载独立清理 |
| UPD-01 | 版本数值比较、草稿/预发布过滤、缺资产、限流/304、网络/兼容性、下载恢复与校验、安装权限/取消/结果、升级后数据保留 |

## 3. 六来源联调检查表

每行在 D1/D3 填真实日期和结果。仅匿名探测与夹具通过不能标记完整来源准入。

| 来源 | 地址/搜索 | 详情/分页 | 认证与过期 | 首中末图片 | 下载/离线 | 当前状态 |
|---|---|---|---|---|---|---|
| picacg | 匿名 API 探测/搜索夹具通过 | 完整分页夹具通过 | 原生登录/过期/资料与打卡合同；真实账号待测 | 平台图片待测 | 合成队列/离线通过，平台待测 | 已实现，待完整准入 |
| EH/EX | 匿名搜索与夹具通过 | 全分页/游标夹具通过 | EH/EX 独立权限夹具 | 额度/原图回退夹具；真实待测 | 共用离线链路，平台待测 | 已实现，待完整准入 |
| JM | Cookie profile/加密域名列表实测 | 详情/章节结构通过 | 独立资料/签到合同；真实账号待测 | 合成图重排通过，平台待测 | 共用离线链路，平台待测 | 已实现，待完整准入 |
| Hitomi | 当前索引/动态规则实测 | 真实匿名元数据与夹具 | 无平台账号；结构失效停止 | 格式与 HEAD，平台解码待测 | 共用离线链路，平台待测 | 已实现，待完整准入 |
| htcomic | 可信发布页/域名实测 | 匿名元数据及完整页数 | 密码/Cookie 原点隔离夹具 | 平台图片待测 | 共用离线链路，平台待测 | 已实现，待完整准入 |
| NH | v2 匿名接口实测 | 分页/详情夹具通过 | Key/User 分槽与权限夹具 | 平台图片待测 | 共用离线链路，真实权限待测 | 已实现，待完整准入 |

## 4. 工程验证层次

- 单元/夹具：签名时钟、JSON/HTML 解析、JM 图像排列、分页、错误分类、Cookie 匹配、会话 single-flight、进度映射。
- 网络集成：MockWebServer 覆盖 401/403/429/重定向/断流；真实来源样本仅作为额外联调，不让 CI 持续刷站。
- Android UI：主要导航、返回栈、登录表单、阅读跳页、下载状态；快照只用于稳定布局，不硬编码网络内容。
- 构建：建工程后执行 `testDebugUnitTest`、`lintDebug`、`assembleDebug`；正式包另验 R8 后真实图片处理/登录回调。
- 设备：API 26 与当前目标系统，至少一台实体手机；360/390/430 dp、横屏、600/800 dp、大字体、深色、分屏。
- 原型：浏览器交互与截图只证明 HTML 行为，不代替以上 Android/平台检查。

## 5. 主要风险及处理

| 风险 | 处理/退出条件 |
|---|---|
| 协议漂移或参考实现冲突 | profile 明确版本；联调失败时标未接入，不猜测成功 |
| 域名更换与登录主机变化 | 候选与信任范围分开，匿名探测后再发送凭据 |
| 网页验证无法在 WebView 完成 | 明确告知限制，平台支持的回调或手动导入；不承诺万能解决 |
| 图片额度/429 | 源级暂停和退避，低预取，不不断换地址重试 |
| 长图/JM 重组内存峰值 | 单任务处理、尺寸预算、区域/条带处理、真机压力测试 |
| 平板/旋转布局 | 约束宽度与自适应网格、进度恢复，不额外扩展功能范围 |

## 6. 实现前的评审项

确认 HTML 的导航与阅读交互；确认首版 P0/P1 范围；确定包名/最低系统及 GitHub 发布仓库、正式签名。发布渠道采用 GitHub Releases，按[更新规范](11-应用更新与GitHub发布.md)落地。认证和网络方案按本稿推进技术验证，待真实平台证据再冻结具体地址和协议 profile。

## 新增设置验收门槛

按[设置规格验收矩阵](12-设置与偏好规格.md#8-开发与验收)验证全部新增项：筛选与平台隔离、签到去重、预加载/音量键/常亮/手势/自动翻页、主题与刷新率、目录/并行/缓存、日志、导入导出、WebDAV 失败与冲突、启动更新。浏览器演示通过不能替代 Android 设备、真实平台和 WebDAV 服务端验收。
